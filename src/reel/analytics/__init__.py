"""Cassette analytics — filtering, pretty-printing, cost, stats, diff.

Pure functions over :class:`CassetteEntry` sequences. No I/O here — the CLI
layer owns reading cassettes and rendering tables.
"""
